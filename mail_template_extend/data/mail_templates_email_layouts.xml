<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Odoo 19 mail_notification_invite still anchors on the footer block, so
         removing it here breaks the upstream mail layout inheritance chain. -->
</odoo>
